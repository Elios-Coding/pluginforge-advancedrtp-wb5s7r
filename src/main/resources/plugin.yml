name: AdvancedRtp
version: 1.0
main: com.pluginforge.advancedrtp.AdvancedRtpPlugin
api-version: 1.21
author: PluginForgeAI
description: Generated by PluginForge AI (freeform mode).
commands:
  rtp:
    description: "Random teleport with admin range controls, cooldowns, warmups, and combat restrictions."
    usage: "/rtp [admin|admins]"
permissions:
  rtp.use:
    description: "Allows using /rtp"
    default: true
  rtp.admin:
    description: "Allows opening the RTP range admin GUI"
    default: op
  rtp.bypass.cooldown:
    description: "Bypasses RTP cooldown"
    default: op
  rtp.bypass.warmup:
    description: "Bypasses RTP warmup"
    default: op
